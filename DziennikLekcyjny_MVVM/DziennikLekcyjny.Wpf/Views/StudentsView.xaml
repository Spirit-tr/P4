<UserControl x:Class="DziennikLekcyjny.Wpf.Views.StudentsView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml">
    <Grid Margin="10">
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="2*"/>
            <ColumnDefinition Width="12"/>
            <ColumnDefinition Width="320"/>
        </Grid.ColumnDefinitions>
        <Grid.RowDefinitions>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <DataGrid Grid.Column="0" ItemsSource="{Binding Students}" SelectedItem="{Binding SelectedStudent}"
                  AutoGenerateColumns="False" IsReadOnly="True" SelectionMode="Single" Margin="0,0,0,8">
            <DataGrid.Columns>
                <DataGridTextColumn Header="ID" Binding="{Binding IdStudenta}" Width="55"/>
                <DataGridTextColumn Header="Imię" Binding="{Binding Imie}" Width="*"/>
                <DataGridTextColumn Header="Nazwisko" Binding="{Binding Nazwisko}" Width="*"/>
                <DataGridTextColumn Header="PESEL" Binding="{Binding Pesel}" Width="120"/>
                <DataGridTextColumn Header="Data ur." Binding="{Binding DataUrodzenia, StringFormat=d}" Width="100"/>
                <DataGridTextColumn Header="Klasa" Binding="{Binding NazwaKlasy}" Width="80"/>
            </DataGrid.Columns>
        </DataGrid>

        <Border Grid.Column="2" BorderBrush="#DDD" BorderThickness="1" CornerRadius="6" Padding="12">
            <StackPanel>
                <TextBlock Text="Dane ucznia" FontSize="18" FontWeight="SemiBold" Margin="4,0,4,10"/>
                <TextBlock Text="Imię" Margin="4,2"/>
                <TextBox Text="{Binding Imie, UpdateSourceTrigger=PropertyChanged}"/>
                <TextBlock Text="Nazwisko" Margin="4,2"/>
                <TextBox Text="{Binding Nazwisko, UpdateSourceTrigger=PropertyChanged}"/>
                <TextBlock Text="PESEL" Margin="4,2"/>
                <TextBox Text="{Binding Pesel, UpdateSourceTrigger=PropertyChanged}" MaxLength="11"/>
                <TextBlock Text="Data urodzenia" Margin="4,2"/>
                <DatePicker SelectedDate="{Binding DataUrodzenia}" Margin="4"/>
                <TextBlock Text="Klasa" Margin="4,2"/>
                <ComboBox ItemsSource="{Binding Classes}" SelectedItem="{Binding SelectedClass}"/>

                <WrapPanel Margin="0,12,0,0">
                    <Button Content="Nowy" Command="{Binding NewCommand}"/>
                    <Button Content="Dodaj" Command="{Binding AddCommand}"/>
                    <Button Content="Zapisz" Command="{Binding UpdateCommand}"/>
                    <Button Content="Usuń" Command="{Binding DeleteCommand}"/>
                </WrapPanel>
                <Button Content="Odśwież" Command="{Binding RefreshCommand}" HorizontalAlignment="Left"/>
            </StackPanel>
        </Border>

        <TextBlock Grid.Row="1" Grid.ColumnSpan="3" Text="{Binding Status}" Foreground="DimGray" Margin="4"/>
    </Grid>
</UserControl>
